{
  "name": "@nexus/deploy-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Blue/green deploy for the platform's OWN services (docs/FEATURES.md §11.10) — distinct from services/cicd's canary/blue-green FEATURE, which is a product capability tenants use for THEIR deployments.",
  "bin": {
    "nexus-deploy": "./dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "typescript": "^5.5.4"
  }
}
